[project]
name = "mycards-ai-proxy"
version = "0.1.0"
description = "Tiny FastAPI proxy that answers questions about card transactions using Claude."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "anthropic",
    "pydantic",
    "python-dotenv",
]
